import { TitleHeaderStyles } from './TitleHeader.types'

const styles: TitleHeaderStyles = {
    titleHeader: {
        margin: 0
    },
    titleHeaderWrapper: {
        textAlign: 'center'
    }
}

export default styles