import { DistributionTableStyleProps } from './DistributionTable.types'

const styles: DistributionTableStyleProps = {
    canvas: {
        height: '800px',
        width: '3000px'
    },
    wrapper: {
        overflowX: 'scroll',
        width: '100%'
    }
}

export default styles